/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'i.ytimg.com', 'media.licdn.com', 'aot.edu.in'],
    },
  };
  
  export default nextConfig;
  